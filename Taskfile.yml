version: '3'

tasks:
  dev:
    desc: Run the development server
    cmds:
      - npm run dev

  build:
    desc: Build the project
    cmds:
      - npm run build

  test:
    desc: Run tests
    cmds:
      - npm test

  clean:
    desc: Clean build artifacts
    cmds:
      - rm -rf dist out build
